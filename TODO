[O] General arraysation
[O] Smart pointers for Result
[O] printAlphabet() should sort the output
[O] serializer must do non-blocking reads!
[O] add "basic" testing (for do_square_int)
[O] currently the preprocessing doesn't work better with overriding:
    Example: "range K=1 P(K=1)=(a[K]->END)."
[O] fix memory leaks
[O] aggregate labels and edges when printing with graphviz
[O] Go parallel!
